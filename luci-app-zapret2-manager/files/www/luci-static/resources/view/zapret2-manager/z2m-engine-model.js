'use strict';
'require baseclass';
var PHASES={queued:'В очереди',preflight:'Предварительная проверка',backup:'Резервное копирование',stopping:'Остановка службы',downloading:'Загрузка',verifying:'Проверка пакета',installing:'Установка',restoring:'Восстановление данных',starting:'Запуск службы',postflight:'Итоговая проверка',completed:'Завершено',failed:'Ошибка',rolling_back:'Откат',rolled_back:'Откат выполнен'};
function normalizeStatus(s){s=s||{};return{state:s.state||'engine_missing',installed:s.installed===true,provider:s.provider||null,packageName:s.packageName||null,packageVersion:s.packageVersion||null,nfqws2Version:s.nfqws2Version||null,upstreamCommit:s.upstreamCommit||null,architecture:s.architecture||null,serviceState:s.serviceState||'engine_missing',compatible:s.compatible!==false,compatibilityMessage:s.compatibilityMessage||'',operation:s.operation||null};}
function actions(v){v=v||{};var installed=v.installed===true,busy=v.busy===true,checked=v.checked===true,compatible=v.compatible===true,same=installed&&v.provider===v.selectedProvider,different=installed&&!!v.selectedProvider&&!same,allowed=checked&&compatible&&!busy;return{check:{visible:true,disabled:busy||!v.selectedProvider},install:{visible:!installed,disabled:!allowed},update:{visible:same&&v.updateAvailable===true,disabled:!allowed},switchProvider:{visible:different,disabled:!allowed},remove:{visible:installed,disabled:busy},cancel:{visible:busy&&v.cancellable===true,disabled:false}};}
function statusKind(s){var value=(s||{}).state;if(value==='error'||value==='failed')return'error';if(value==='engine_missing')return'missing';if(value==='operation')return'busy';return'ok';}
return baseclass.extend({phases:PHASES,phaseLabel:function(value){return PHASES[value]||value||'—';},normalizeStatus:normalizeStatus,actions:actions,statusKind:statusKind});
