let obj = { in: 8, else: 12 };
let divisor = 2;
let inValue = obj.in / divisor;
let elseValue = obj.else / divisor;
let spacedValue = obj. in / divisor;
let newlineValue = obj.
else / divisor;
let commentedValue = obj./* property trivia */in / divisor;
let decimalValue = 1.5 / 0.5;
let trailingDecimalValue = 1. / divisor;
