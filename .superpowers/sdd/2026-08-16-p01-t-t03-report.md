# P01-T T03 report — donor log transplant

## TDD evidence

- RED: `node --test tests/ui/avatar-log-transplant.test.mjs` failed 2/2
  before the shared donor-derived module and page adapters existed.
- GREEN: the same focused contract passed 2/2 after implementation.

## Implementation

`z2m-avatar-log.js` retains the frozen donor `logs.js` rendering primitives
(`log-row`, `log-badge`, `log-time`, `log-source`, `log-message`, bounded
viewer, empty state) and exposes `normalizeRows()` plus `renderNormalized()`.
Overview and maintenance now pass normalized Z2M `eventsTail`/event-tail data;
the renderer does not contain donor HTTP/SSE/API code. Technical details are
redacted through `MaintenanceModel` and shown only in advanced mode.

## Verification

- `avatar-log-transplant.test.mjs`: 2/2 passed.
- `node --check`: shared log module, overview, and maintenance passed.
- No donor `/api/` calls or standalone donor backend code were added.
- P02 remains not started; browser and target evidence remain pending.
