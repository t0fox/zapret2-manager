# P01-T T04 report — lifecycle affordance transplant

## TDD evidence

- RED: `node --test tests/ui/avatar-lifecycle-transplant.test.mjs` failed 1/2
  because the Dashboard had no donor provenance marker, spinner, or pending
  accessibility state.
- GREEN: the focused contract passed 2/2 after the lifecycle button adapter
  was updated.

## Implementation

The donor Dashboard `quickAction` pending affordance is retained at the Z2M
boundary: the active button shows the donor spinner pattern, all three action
buttons are disabled while one request is pending, and `aria-busy` plus
`data-lifecycle-pending` expose the state. Z2M `service.start/stop/restart`,
authoritative status convergence, Russian result copy, and reload remain
unchanged owners.

## Verification

- `avatar-lifecycle-transplant.test.mjs`: 2/2 passed.
- Dashboard parity and runtime navigation/lifecycle contracts: 18/18 passed.
- `node --check z2m-overview.js`: passed.
- No donor backend endpoint or second mutation path was added.
